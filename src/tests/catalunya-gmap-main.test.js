import {describe, expect, it} from "@jest/globals"; // Adjust the path as needed

describe('initMapApplication', () => {

    it('successfully initializes the map and hides the error message', async () => {
        expect(true)
    });

});
