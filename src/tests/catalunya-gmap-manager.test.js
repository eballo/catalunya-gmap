import {describe, expect, it} from "@jest/globals"; // Adjust the path as needed

describe('ManagerMap', () => {

    it('successfully initializes the ManagerMap', async () => {
        expect(true)
    });

});
